# Normaliza finales de línea (USA LF en repo; Windows convierte al checkout si aplica)
* text=auto eol=lf

# Archivos binarios (no intentar diffs)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.woff binary
*.woff2 binary

# Scripts: forzamos LF
*.sh text eol=lf

# Lockfile (texto) pero sin CRLF
pnpm-lock.yaml text eol=lf
